$("#ingredient-list").html("<%= escape_javascript render('ingredients') %>");
Materialize.toast('<%= @message %>', 4000)
$("body").animate({scrollTop: $("#new-ingredient-btn").position().top},1000);

enable_form_loader_listener();
